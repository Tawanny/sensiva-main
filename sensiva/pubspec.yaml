name: sensiva
description: A Weed delivery app.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

## UI
  get: ^4.6.6
  gap: ^3.0.1
  iconly: ^1.0.1
  ionicons: ^0.2.2
  cupertino_icons: ^1.0.2
  cached_network_image: ^3.2.3
  floating_bottom_navigation_bar: ^1.5.2
  flutter_staggered_grid_view: ^0.7.0
  fluttericon: ^2.0.0
  google_fonts: ^5.1.0
  ui_common:
    git: https://github.com/brocodev/ui_common.git

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
